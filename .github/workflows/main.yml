name: Daily Commit
on:
  workflow_dispatch:  # Manual trigger only (test this first)
  schedule:
    - cron: "20 18 * * *"  # 11:50 PM IST

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow is working!"
